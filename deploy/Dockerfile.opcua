FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates && rm -rf /var/lib/apt/lists/*
COPY bin/tentacle-opcua /usr/local/bin/tentacle-opcua
RUN chmod +x /usr/local/bin/tentacle-opcua
ENTRYPOINT ["/usr/local/bin/tentacle-opcua"]
