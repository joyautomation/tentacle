# Tentacle Platform Configuration
# Copy this file to tentacle.env and modify as needed.
# All services read from this shared environment file.

# NATS Server
NATS_SERVERS=nats://localhost:4222
# NATS_USER=
# NATS_PASS=
# NATS_TOKEN=

# GraphQL API (tentacle-graphql)
GRAPHQL_PORT=4000
GRAPHQL_HOSTNAME=0.0.0.0
# GRAPHQL_PLAYGROUND=true
# GRAPHQL_INTROSPECTION=true
# GRAPHQL_CORS_ORIGIN=*
TENTACLE_MODE=systemd

# Web Dashboard (tentacle-web)
GRAPHQL_URL=http://localhost:4000/graphql
PORT=3012

# MQTT Sparkplug B Bridge (tentacle-mqtt)
# Uncomment and configure if using tentacle-mqtt
# MQTT_BROKER_URL=mqtt://localhost:1883
# MQTT_CLIENT_ID=tentacle-mqtt
# MQTT_GROUP_ID=TentacleGroup
# MQTT_EDGE_NODE=EdgeNode1
# MQTT_USERNAME=
# MQTT_PASSWORD=

# OPC UA Client (tentacle-opcua)
# OPCUA_PKI_DIR=/opt/tentacle/data/opcua/pki
# OPCUA_AUTO_ACCEPT_CERTS=true
